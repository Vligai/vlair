# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0

# Redis caching (optional but recommended)
redis>=5.0.0

# EML Parser
eml-parser>=1.17.0

# IOC Extractor (no additional dependencies)

# Hash Lookup (no additional dependencies - uses requests, python-dotenv)

# Domain/IP Intelligence (no additional dependencies - uses requests, python-dotenv)

# Log Analysis (no additional dependencies - uses built-in regex and datetime)

# PCAP Analyzer
scapy>=2.5.0
